# Running the numerical experiments

In order to run the numerical experiments, run the following code from a new virtual environment:

```
pip install -r requirements.txt
python STRONG_monotonicity_default.py
```
These scripts will generate in the local folder the four plots of the paper (Fig. 1).
